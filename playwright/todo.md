# TODO LIST

## Problems
1. First login unsuccesful, returns error page
2. Subsequent logins take previous caches, thus permanently logged in
> Check if token exists first? If exist, use and enable features? If doesn't exist, logout and retry?
3. Determining success failed despite TRUE
> Again, check token exists first?
